# Skill 根目录 .env 中的同名值优先于系统环境变量。
WORKBENCH_BASE_URL=https://workbench.example.com
WORKBENCH_USERNAME=replace-with-username
WORKBENCH_PASSWORD=replace-with-password

# 非 HTTP Basic Auth 时，清空上方账密并改用完整请求头。
# WORKBENCH_AUTH_HEADER=Authorization: Bearer replace-with-token
